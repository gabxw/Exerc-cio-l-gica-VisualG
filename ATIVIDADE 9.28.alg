algoritmo "Nome do Programa"

var
 x, c, multiplo:inteiro


inicio
multiplo <- 1
escreval("Digite um número: ")
leia(x)

para c de 1 ate 10 faca
escreval(x, " x ", c, " = ", x * multiplo )
multiplo <- multiplo + 1
fimpara


fimalgoritmo